#include "get_next_line.h"

int	add_check2(char **choto, char *temp)
{
	if (!(*choto))
		return (-1);
	if (temp)
		free(temp);
	return (0);
}

int	add_check3(char **choto, char *temp)
{
	if (!(*choto))
		return (-1);
	if (temp)
		free(temp);
	return (1);
}
